/**
 * This class is a temporary file until the file event.ts is renamed
 */

import { EventDay } from './event-day';

/**
 * EventInfo ( event is reserved ) - ( modify this as appropriate, it still has no views )
 */
class EventInfo {
    name: String;
    description: String;
    dayDetails: EventDay[];
}